import { makeStyles } from "@mui/styles";

const useStyle = makeStyles({
  header: {
    marginTop: "30px",
    margin: " 0 auto",
    width: "70%",
  },
  admin: {
    textAlign: "right",
    fontWeight: "bold",
    fontSize: "23px",
  },
});

export default useStyle;
