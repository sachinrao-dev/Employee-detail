import { makeStyles } from "@mui/styles";

const useStyle = makeStyles({
  header: {
    marginTop: "30px",
  },
  admin: {
    textAlign: "right",
    fontWeight: "bold",
    fontSize: "23px",
  },
});

export default useStyle;
